import { structure } from '../../../helpers/structure';
import { module, test } from 'qunit';

module('Unit | Helper | structure');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = structure([42]);
  assert.ok(result);
});
