import injectLayout from '../../../helpers/-inject-layout';
import { module, test } from 'qunit';

module('Unit | Helper |  inject layout');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = injectLayout.create({});
  assert.ok(result);
});
