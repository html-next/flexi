<box xs="10" class="offset-xs-1" vertical>
  <hbox fit>
    <h1>Layout Service</h1>
    <hbox align="center" justify="end">
      <span>Next <LinkTo @route="docs.blueprints">Blueprints</LinkTo></span>
    </hbox>
  </hbox>

  {{#let (get-code-snippet "install-1.js") as |snippet|}}
    <pre class={{snippet.language}}>{{snippet.source}}</pre>
  {{/let}}

  <p>
    Once you have installed flexi, you will also need to install the shim for ember-app. in your ember-cli-build.js
    file. This shim makes ember-cli's template tree able to find the templates for layouts.
  </p>

  {{#let (get-code-snippet "install-2.js") as |snippet|}}
    <pre class={{snippet.language}}>{{snippet.source}}</pre>
  {{/let}}
</box>